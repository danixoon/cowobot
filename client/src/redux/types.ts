export type PPOPUP = {
  ADD: "@prefix/ADD";
};

export const ADD = "@prefix/ADD"; // => '@prefix/ADD'

export const POPUP = {
  ADD: "POPUP.ADD" as "POPUP.ADD",
  PUSH: "POPUP.REMOVE" as "POPUP.REMOVE",
};

export const TYPES = {
  POPUP,
};
